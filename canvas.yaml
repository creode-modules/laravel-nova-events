preset: package

namespace: Creode\LaravelNovaEvents
user-auth-model: App\Models\User

paths:
  src: src
  resource: resources

factory:
  path: database/factories

migration:
  path: database/migrations
  prefix: ''

console:
  namespace: Creode\LaravelNovaEvents\Console

model:
  namespace: Creode\LaravelNovaEvents\Entities
  path: src/Entities

provider:
  namespace: Creode\LaravelNovaEvents\Providers

testing:
  namespace: Creode\LaravelNovaEvents\Tests
